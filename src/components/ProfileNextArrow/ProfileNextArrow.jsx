import React from 'react';
import './ProfileNextArrow.css';
const ProfileNextArrow = ({ className, style, onClick }) => {
  return (
    <button
      className='profile-next-arrow'
      onClick={onClick}
    >

    </button>
  );
}

export default ProfileNextArrow;
