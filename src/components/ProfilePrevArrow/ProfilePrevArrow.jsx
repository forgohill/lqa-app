import React from 'react';
import './ProfilePrevArrow.css';
const ProfilePrevArrow = ({ className, style, onClick }) => {
  return (
    <button
      className='profile-prev-arrow'
      onClick={onClick}
    >

    </button>
  );
}

export default ProfilePrevArrow;
